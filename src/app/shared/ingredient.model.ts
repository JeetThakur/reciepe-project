export class Ingredient {
  //     // how should it look like
  //     public name: string;
  //     public amount: number;

  // this will directly create the above two attributes and also provide them with the supplied value at constructor
  // initialization
  constructor(public name: string, public amount: number) {}
}
